export enum ButtonVariant {
    Primary = "primary",
    Secondary = "secondary", // currently not used
}

export enum ButtonColor {
    Primary = "primary",
    Ghost = "ghost",
    Secondary = "magenta",
}

export enum ButtonSize {
    Normal = "normal",
}
