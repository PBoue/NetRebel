export enum TeaserSize {
    Default = "default",
    Full = "full",
}

export enum TeaserImagePosition {
    Left = "left",
    Right = "right",
}

export enum TeaserHeadlineSize {
    Default = "default",
    Medium = "medium",
    Large = "large",
}

export enum TeaserTextPosition {
    Top = "top",
    Center = "center",
    Bottom = "bottom",
}
